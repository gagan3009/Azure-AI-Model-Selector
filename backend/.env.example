# Azure AI Model Selection - Environment Variables

# PostgreSQL connection (enables database backend)
# When set, the app uses PostgreSQL for CRUD operations
# When unset, the app falls back to data/models.json (read-only)
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/aimodels

# Azure subscription (enables live Azure features)
AZURE_SUBSCRIPTION_ID=
